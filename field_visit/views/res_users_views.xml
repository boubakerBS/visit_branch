<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="view_users_form_field_visit" model="ir.ui.view">
        <field name="name">res.users.form.field.visit</field>
        <field name="model">res.users</field>
        <field name="inherit_id" ref="base.view_users_form"/>
        <field name="arch" type="xml">
            <xpath expr="//notebook" position="inside">
                <page string="Field Visit" name="field_visit" groups="field_visit.group_field_supervisor">
                    <group>
                        <field name="field_visit_branch_ids"
                               widget="many2many_tags"
                               placeholder="Select the branch(es) this user supervises..."/>
                    </group>
                    <p class="text-muted">
                        Only relevant for users in the "Branch Supervisor"
                        or "Administrator" group. A supervisor only sees
                        delegates, customers, plans and visits belonging
                        to the branches listed here.
                    </p>
                </page>
            </xpath>
        </field>
    </record>

</odoo>
